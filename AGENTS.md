# 项目协作规则

## Git 提交与推送

- 未经用户明确要求，不主动执行 `git commit`。
- 只有当用户明确说“提交”时，才按照用户指定的提交点和提交规则执行提交。
- 如果用户只说“提交”，但没有说明提交范围或提交信息，先检查变更并给出建议，再等待用户确认。
- 不执行 `git push`。推送由用户人工完成。

## 工作方式

- 每次改动前先说明将要修改什么。
- 文档、学习笔记、代码和配置都应保持小步推进。
- 发现当前变更中有不属于本次任务的内容时，不回滚，先说明情况。
