# 第二阶段计划表（阶段 5：Agent 能力增强 + 阶段 6：作品化）

> 本表为第二阶段权威执行基准，后续严格按表推进。每完成一步更新「状态」与记忆。
> 总体精神：延续第一期「大框架、小闭环」——保持落库契约稳定，外部 API 不盲目接，前端保持轻薄。

## 已锁定决策
- LLM：本地 Ollama（OpenAI 兼容），结构化输出复用现有 Pydantic Schema，Java 落库契约不变。
- 前端：独立 **React + Vite + TypeScript** SPA（`frontend/`），只调 **Java REST**；开发期 Vite proxy 代理 `/api`，不改 Java CORS。
- 外部 API（图片/物流）策略：**到达对应步骤时再定**（先 LLM+规则模拟，或接真实服务），此决策影响步骤 2/4 范围与是否新增表。
- 降级：LLM / 外部调用失败一律降级到规则实现并继续，不中断主链路。

## 计划表

| 序号 | 名称 | 目标 | 范围（做 / 不做） | 关键决策 / 依赖 | 验收标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 0 | LLM 打底 | 5 个 Agent 接真实 LLM + 规则降级 | 做：LLM 客户端 + 结构化输出 + 降级追踪。不做：单 Agent 深度增强 | 已完成 `61bb0b7` | `uv run pytest` 12 passed；LLM 失败降级 | ✅ 完成 |
| 0.5 | 前端只读 SPA | 人工阅读审核入口 | 做：运营计划列表/详情 + Agent trace + 商品/库存/订单查看。不做：写操作 | 已完成 `0ed67df`；Vite proxy | `npm run build` 通过；dev 200；可看计划与 trace | ✅ 完成 |
| 1 | Product Planning 增强 | 平台化文案 + SEO | 做：增强 prompt；扩展 `ProductPlan` 增加 SEO/多平台文案字段。不做：真实图片 | 已完成 `c70b6d1`；**扩展不影响 Java 落库**（product_plan_json 为通用 Map） | 产出含 SEO 关键词与平台适配文案；测试覆盖；规则 fallback 可用 | ✅ 完成 |
| 2 | 分类知识库 RAG（横向） | 检索增强，赋能所有 Agent | 做：沉淀品类/平台规则/违禁词，检索注入 prompt。不做：复杂知识图谱 | 决策：**Markdown 分品类** + **本地向量 RAG**（`nomic-embed-text` 嵌入 + 内存 Chroma）；Python 侧、不改 Java/Postgres、不持库 | Agent 回答引用知识库内容；可检索；`uv run pytest` 全绿 | ✅ 完成（commit 待用户提交） |
| 3 | Image Creative 增强 | 真实图片生成 / 审核 | 做：文生图 API 或视觉审核，返回 URL/Base64 或审核结果。不做：本地无算力硬上 | 决策：生成 vs 审核 vs 都要；提供方（费用/key/本地）；图存储位置 | 返回真实图片或审核结果；失败降级不阻断 | ⬜ 待做（依赖外部 API 策略） |
| 4 | Inventory & Purchase 增强 | 库存预测 + 采购周期 | 做：基于销量历史的预测（移动平均/指数平滑）+ LLM 建议。不做：复杂时序模型 | 决策：统计模型 vs LLM 启发式；**是否扩充历史销量字段（影响 Schema/落库）** | 预测需求量 + 建议补货，优于阈值规则 | ⬜ 待做 |
| 5 | Order Fulfillment 增强 | 物流异常 + 售后联动 | 做：物流异常检测（规则/LLM）+ 处理建议。不做：强行接真实物流 | 决策：是否接真实物流 API；**售后是否建模（可能新增表）** | 识别物流风险并给处理建议 | ⬜ 待做（依赖外部 API 策略） |
| 6 | Supervisor 动态路由 + 前端确认 UI | 编排智能化 + 人工确认闭环 | 做：条件路由替代固定顺序 + 失败重试 + 人工确认；前端确认/驳回 UI（新增确认 API）。不做：过度复杂状态机 | 决策：LangGraph vs 轻量 Python 路由；人工确认存储方式 | 编排可跳过/重试/转人工；trace 体现路由；前端可确认 | ⬜ 待做（依赖 0.5 基础） |
| 7 | 阶段 6 作品化 | 可展示作品 | 做：Docker Compose 一键启动含前端构建；README 演示；面试讲解文档；示例数据；链路截图。不做：额外业务功能 | 依赖：前述全部；跨域最终方案（CORS / 反代） | 一键启动全链路；演示与面试文档齐备 | ⬜ 待做 |

## 执行顺序（锁定）
```
0 (LLM 打底) ✅
0.5 (前端只读) ✅
1 (Product Planning 增强) ✅
2 (分类知识库 RAG) ✅
3 (Image Creative 增强)
4 (Inventory & Purchase 增强)
5 (Order Fulfillment 增强)
6 (Supervisor 动态路由 + 前端确认 UI)
7 (阶段 6 作品化)
```
- 步骤 3/4/5 的「外部 API 策略」在各自开工前拍板（模拟 or 真实）。
- 步骤 2（知识库）可穿插到步骤 1 起赋能，不阻塞主线。
- 每步开工前先进入计划模式细化方案，确认后再写码（延续「不盲目下手」）。

## 通用约束
- 扩展落库字段（Schema）须评估 Java 侧影响，优先向后兼容。
- 任一外部/LLM 调用须有规则 fallback，失败记入 `errors` 且不中断主链路。
- 前端保持轻薄，不引重型 UI 库、不引入登录鉴权（除非步骤 6 另有需要）。
